# 🚀 977-361 Software Architecture and Design: Docker Compose Counter App

โปรเจกต์นี้จัดทำขึ้นเพื่อสาธิตการใช้งาน **Docker Compose** ในการ Deploy แอปพลิเคชันแบบ 3-Tier (Nginx, FastAPI, Redis) และการจัดการ Source Code ด้วย **Git**

---

## 💻 Tech Stack & Architecture

| Component | Technology | Role |
| :--- | :--- | :--- |
| **Frontend/Proxy** | **Nginx** | Reverse Proxy และเสิร์ฟ Static HTML/JS |
| **Backend/API** | **FastAPI (Python)** | จัดการตรรกะการเพิ่มตัวนับ (Counter Logic) |
| **Database/Cache** | **Redis** | จัดเก็บค่าตัวนับให้คงอยู่ถาวร (Persistent Counter) |
| **Deployment** | **Docker Compose** | จัดการและเชื่อมต่อ 3 คอนเทนเนอร์เข้าด้วยกัน |
| **Version Control** | **Git / Docker Hub** | จัดการประวัติโค้ดและเผยแพร่ Image |

---

## 🛠️ วิธีการ Deploy และใช้งาน

### 1. การเตรียมตัว

* ติดตั้ง Docker และ Docker Compose v2 บนเครื่อง Host
* Login เข้า Docker Hub และ GitHub CLI เรียบร้อยแล้ว

### 2. การรันโปรเจกต์

โปรเจกต์นี้จะดึง Image ของ Backend มาจาก Docker Hub โดยตรง (Image: `rachata14/l03-fastapi-redis:v1`) และรันบนพอร์ต 8080

```bash
# 1. เข้าสู่โฟลเดอร์โปรเจกต์
cd your-project-folder/

# 2. รันระบบทั้งหมดในโหมดพื้นหลัง
# คำสั่งนี้จะทำการ Pull Image จาก Docker Hub และ Start ระบบ
docker compose up -d 

# 3. ดู Log เพื่อตรวจสอบสถานะ
docker compose logs -f
